/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "dvd6ljcj7w3pj.cloudfront.net",
        port: "",
      },
    ],
  },
};

export default nextConfig;
